import { openModal, closeModal } from '../../actions/modal/modal_actions';

const modalReducer = (state, action) => {
  Object.freeze(state);
  switch (action.type) {
    default:
      return state;
  }
};
